# Rust
target/

# Node / frontend
node_modules/
activity/dist/

# Local data (dev runs)
data/
*.db
*.db-shm
*.db-wal

# Planning & docs — kept local only, not committed.
# (Includes docs/reference/, the vendored Svelte docs; restore with:
#  curl -sL https://svelte.dev/llms-full.txt -o docs/reference/svelte-llms-full.txt)
/PLAN.md
/docs/

# Secrets
leaf.conf
.env
